import { useState } from "react";
import { Link, useNavigate, useSearchParams } from "react-router-dom";
import { Sparkles } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { api } from "@/services/api";
import { useAuth } from "@/contexts/AuthContext";

export default function Auth(){const [params]=useSearchParams(); const mode=params.get("mode")||location.pathname.split('/').pop()||"login"; const nav=useNavigate(); const {login}=useAuth(); const [form,setForm]=useState<any>({}); const [resetToken,setResetToken]=useState(""); const submit=async(e:React.FormEvent)=>{e.preventDefault(); if(mode==="login"){await login(form.email,form.password); nav("/");} else if(mode==="register"){await api.signup(form); toast.success("Account created. Please log in."); nav("/auth/login");} else if(mode==="forgot"){const r=await api.forgot(form.email); setResetToken(r.Token); toast.success(r.message);} }; const set=(k:string,v:string)=>setForm({...form,[k]:v}); return <div className="section-shell grid min-h-[calc(100vh-5rem)] items-center gap-8 py-10 lg:grid-cols-2"><div className="rounded-3xl glass-panel p-8"><h1 className="text-5xl font-bold text-secondary">{mode==="register"?"Create Account":mode==="forgot"?"Reset Password":"Welcome Back"}</h1><form onSubmit={submit} className="mt-6 grid gap-4">{mode==="register"&&<><input required className="rounded-xl border border-input bg-background p-3" placeholder="Name" onChange={e=>set("name",e.target.value)}/><input required className="rounded-xl border border-input bg-background p-3" placeholder="Username" onChange={e=>set("username",e.target.value)}/></>}<input required className="rounded-xl border border-input bg-background p-3" type="email" placeholder="Email" onChange={e=>set("email",e.target.value)}/>{mode!=="forgot"&&<><input required className="rounded-xl border border-input bg-background p-3" type="password" placeholder="Password" onChange={e=>set("password",e.target.value)}/>{mode==="register"&&<input required className="rounded-xl border border-input bg-background p-3" type="password" placeholder="Confirm Password" onChange={e=>set("confirmPassword",e.target.value)}/>}</>}<Button variant="hero" size="lg">{mode==="register"?"Sign Up":mode==="forgot"?"Send Code":"Login"}</Button></form>{mode==="forgot"&&resetToken&&<div className="mt-5 rounded-2xl border border-border p-4"><input className="mb-3 w-full rounded-xl border border-input bg-background p-3" placeholder="6-digit code" onChange={e=>set("resetcode",e.target.value)}/><Button variant="glass" onClick={()=>api.verifyCode(resetToken,form.resetcode).then(()=>toast.success("Code verified"))}>Verify Code</Button><input className="mb-3 mt-3 w-full rounded-xl border border-input bg-background p-3" type="password" placeholder="New password" onChange={e=>set("newPassword",e.target.value)}/><Button onClick={()=>api.resetPassword(resetToken,{password:form.newPassword,confirmPassword:form.newPassword}).then(()=>nav('/auth/login'))}>Reset Password</Button></div>}<div className="mt-5 flex flex-wrap gap-3 text-sm"><Link to="/auth/register" className="text-secondary">Sign Up</Link><Link to="/auth/login" className="text-secondary">Login</Link><Link to="/auth/forgot" className="text-secondary">Forgot Password?</Link></div><Button variant="glass" className="mt-4 w-full" onClick={api.google}>Continue with Google</Button></div><div className="rounded-3xl bg-blush p-10 shadow-glow"><Sparkles className="h-12 w-12 text-secondary"/><h2 className="mt-4 text-5xl font-bold text-secondary">Your soft-glam ritual, beautifully kept.</h2><p className="mt-4 text-muted-foreground">Save favorites, review products, and checkout with a luxurious account experience.</p></div></div>}
