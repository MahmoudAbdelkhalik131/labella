import { useQuery } from "@tanstack/react-query";
import { Link } from "react-router-dom";
import { ShieldCheck, RotateCcw, LockKeyhole, ArrowRight } from "lucide-react";
import hero from "@/assets/make-it-real-hero.jpg";
import { api } from "@/services/api";
import type { ApiList, Category, Product } from "@/services/types";
import { Button } from "@/components/ui/button";
import { ProductCard } from "@/components/storefront/ProductCard";
import { EmptyState, ProductSkeletonGrid, QuickView, SectionTitle } from "@/components/storefront/StoreUi";
import { useState } from "react";

export default function Home() { const [quick,setQuick]=useState<Product|null>(null); const categories=useQuery({queryKey:["categories"],queryFn:()=>api.get<ApiList<Category>>("/categories")}); const trending=useQuery({queryKey:["products","trending"],queryFn:()=>api.get<ApiList<Product>>("/products?sort=-sold&limit=8")}); const arrivals=useQuery({queryKey:["products","arrivals"],queryFn:()=>api.get<ApiList<Product>>("/products?sort=-createdAt&limit=8")}); const sale=(arrivals.data?.data||[]).filter(p=>p.priceAfterDiscount && p.priceAfterDiscount<p.price);
return <><section className="section-shell grid min-h-[calc(100vh-5rem)] items-center gap-8 py-8 lg:grid-cols-[0.9fr_1.1fr]"><div className="animate-fade-up space-y-7"><p className="font-semibold uppercase tracking-wider text-secondary">Beauty that feels like you</p><h1 className="text-5xl font-extrabold leading-tight text-secondary md:text-7xl">Discover Your True Glow</h1><p className="max-w-xl text-lg text-muted-foreground">Curated makeup, skin rituals, and glow essentials wrapped in a warm luxury shopping experience.</p><div className="flex flex-wrap gap-3"><Button asChild variant="hero" size="lg"><Link to="/shop">Shop Now <ArrowRight/></Link></Button><Button asChild variant="glass" size="lg"><Link to="/categories">Explore Categories</Link></Button></div></div><div className="relative animate-fade-up [animation-delay:120ms]"><div className="absolute -inset-4 rounded-[2rem] bg-blush opacity-70 blur-2xl"/><img src={hero} alt="Make It Real blush cosmetics collection" width={1600} height={960} className="relative aspect-[5/4] w-full rounded-[2rem] object-cover shadow-glow"/></div></section><div className="overflow-hidden border-y border-border bg-secondary py-3 text-secondary-foreground"><div className="flex w-max animate-marquee gap-10 text-sm font-semibold uppercase tracking-widest"><span>Free Shipping • New Arrivals • Best Sellers • Clean Glow Picks • Free Returns • Secure Payment • </span><span>Free Shipping • New Arrivals • Best Sellers • Clean Glow Picks • Free Returns • Secure Payment • </span></div></div><section className="section-shell py-16"><SectionTitle eyebrow="Browse" title="Featured Categories"/><div className="flex gap-4 overflow-x-auto pb-4">{categories.data?.data?.map(c=><Link key={c._id} to={`/shop?category=${c._id}`} className="group min-w-48 overflow-hidden rounded-2xl glass-panel"><img src={c.image||hero} alt={c.name} loading="lazy" className="h-40 w-full object-cover transition-transform group-hover:scale-105"/><div className="p-4 font-bold text-secondary">{c.name}</div></Link>)}</div></section><section className="section-shell py-10"><SectionTitle eyebrow="Loved now" title="Trending Best Sellers"><Button asChild variant="glass"><Link to="/shop?sort=-sold">View all</Link></Button></SectionTitle>{trending.isLoading?<ProductSkeletonGrid/>:<div className="grid grid-cols-2 gap-4 lg:grid-cols-4">{trending.data?.data?.map(p=><ProductCard key={p._id} product={p} onQuickView={setQuick}/>)}</div>}</section><section className="section-shell py-10"><SectionTitle eyebrow="Fresh drops" title="New Arrivals"/>{arrivals.isLoading?<ProductSkeletonGrid/>:<div className="grid grid-cols-2 gap-4 lg:grid-cols-4">{arrivals.data?.data?.map(p=><ProductCard key={p._id} product={p} onQuickView={setQuick}/>)}</div>}</section><section className="section-shell py-12"><div className="rounded-[2rem] bg-plum p-8 text-secondary-foreground shadow-glow md:p-12"><h2 className="text-4xl font-bold">Soft-glam offers are glowing</h2><p className="mt-3 max-w-2xl opacity-90">Limited-time discounts on radiant essentials.</p><div className="mt-6">{sale[0]?<Button asChild variant="gold"><Link to={`/products/${sale[0]._id}`}>Shop the offer</Link></Button>:<Button asChild variant="gold"><Link to="/shop">Find offers</Link></Button>}</div></div></section><section className="section-shell grid gap-4 py-12 md:grid-cols-3">{[[ShieldCheck,"100% Authentic"],[RotateCcw,"Free Returns"],[LockKeyhole,"Secure Payment"]].map(([Icon,label])=><div key={String(label)} className="rounded-2xl glass-panel p-6 text-center"><Icon className="mx-auto mb-3 h-8 w-8 text-secondary"/><h3 className="font-bold text-secondary">{String(label)}</h3></div>)}</section><section className="section-shell py-12"><EmptyState title="Join the glow list" text="Newsletter signup is ready for your API hook — collect emails for launches and rituals." action={<div className="mx-auto flex max-w-md gap-2"><input className="min-w-0 flex-1 rounded-xl border border-input bg-background px-4" placeholder="Email address"/><Button>Subscribe</Button></div>}/></section><QuickView product={quick} open={!!quick} onOpenChange={(o)=>!o&&setQuick(null)}/></> }
