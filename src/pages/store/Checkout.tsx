import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { CheckCircle2 } from "lucide-react";
import { toast } from "sonner";
import { api } from "@/services/api";
import type { Address, Cart } from "@/services/types";
import { Button } from "@/components/ui/button";
import { money } from "@/components/storefront/ProductCard";

export default function Checkout(){const [step,setStep]=useState(1); const [selected,setSelected]=useState(""); const [address,setAddress]=useState({street:"",city:"",state:"",zip:""}); const addresses=useQuery({queryKey:["addresses"],queryFn:()=>api.get<{data?:Address[]}|Address[]>("/address")}); const cart=useQuery({queryKey:["checkout-cart"],queryFn:()=>api.get<Cart>("/cart")}); const list=Array.isArray(addresses.data)?addresses.data:(addresses.data?.data||[]); const save=async(e:React.FormEvent)=>{e.preventDefault(); await api.post("/address",{address}); toast.success("Address saved"); setAddress({street:"",city:"",state:"",zip:""}); addresses.refetch();}; const place=async()=>{if(!selected) return toast.error("Save and select an address first"); await api.post("/order",{address:selected}); toast.success("Order placed"); setStep(3);}; return <div className="section-shell py-10"><h1 className="mb-8 text-5xl font-bold text-secondary">Checkout</h1><div className="mb-8 grid gap-3 md:grid-cols-3">{["Address","Payment","Confirmation"].map((s,i)=><div key={s} className="rounded-2xl glass-panel p-4 font-bold text-secondary"><span className="mr-2">{i+1}</span>{s}</div>)}</div>{step<3?<div className="grid gap-6 lg:grid-cols-[1fr_360px]"><section className="rounded-2xl glass-panel p-6"><h2 className="text-2xl font-bold text-secondary">Saved Addresses</h2><div className="mt-4 grid gap-3">{list.map(a=><label key={a._id} className="flex gap-3 rounded-xl border border-border p-4"><input type="radio" name="address" value={a._id} onChange={()=>setSelected(a._id)}/><span>{a.street}, {a.city}, {a.state} {a.zip}</span></label>)}</div><form onSubmit={save} className="mt-6 grid gap-3 sm:grid-cols-2"><input required placeholder="Street" className="rounded-xl border border-input bg-background p-3" value={address.street} onChange={e=>setAddress({...address,street:e.target.value})}/><input required placeholder="City" className="rounded-xl border border-input bg-background p-3" value={address.city} onChange={e=>setAddress({...address,city:e.target.value})}/><input required placeholder="State" className="rounded-xl border border-input bg-background p-3" value={address.state} onChange={e=>setAddress({...address,state:e.target.value})}/><input required placeholder="ZIP" className="rounded-xl border border-input bg-background p-3" value={address.zip} onChange={e=>setAddress({...address,zip:e.target.value})}/><Button className="sm:col-span-2" variant="glass">Save Address</Button></form></section><aside className="h-fit rounded-2xl glass-panel p-6"><h2 className="text-2xl font-bold text-secondary">Cash on Delivery</h2><p className="mt-2 text-muted-foreground">Current supported payment method.</p><div className="mt-6 space-y-2 text-sm">{cart.data?.items?.map(i=><p key={i.product._id} className="flex justify-between"><span>{i.product.name} × {i.quantity}</span><b>{money(i.price*i.quantity)}</b></p>)}<p className="flex justify-between border-t border-border pt-3 text-lg"><span>Total</span><b>{money(cart.data?.totelPriceAfterDiscount||cart.data?.totelPrice)}</b></p></div><Button variant="hero" className="mt-6 w-full" onClick={place}>Place Order</Button></aside></div>:<div className="rounded-3xl glass-panel p-10 text-center"><CheckCircle2 className="mx-auto h-16 w-16 text-secondary"/><h2 className="mt-4 text-4xl font-bold text-secondary">Order confirmed</h2><p className="mt-2 text-muted-foreground">Your glow ritual is on its way.</p></div>}</div>}
