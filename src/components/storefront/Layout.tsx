import { Heart, Home, Menu, Search, ShoppingBag, Store, User, X, Instagram, Facebook, Twitter } from "lucide-react";
import { Link, NavLink, Outlet, useNavigate } from "react-router-dom";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { useAuth } from "@/contexts/AuthContext";
import { useShop } from "@/contexts/ShopContext";
import { cn } from "@/lib/utils";

const nav = [{ to: "/", label: "Home" }, { to: "/shop", label: "Shop" }, { to: "/categories", label: "Categories" }, { to: "/about", label: "About" }];
export function Layout() { const [menu,setMenu]=useState(false); const [language,setLanguage]=useState("EN"); const [q,setQ]=useState(""); const navigate=useNavigate(); const { user, logout }=useAuth(); const { cartCount, wishlistIds, cartPulse }=useShop(); const submit=(e:React.FormEvent)=>{e.preventDefault(); if(q.trim()) navigate(`/search?search=${encodeURIComponent(q.trim())}`)};
  return <div className="min-h-screen pb-20 md:pb-0"><header className="sticky top-0 z-40 border-b border-border/60 bg-glass/80 backdrop-blur-xl"><div className="section-shell flex h-20 items-center gap-4"><Button variant="ghost" size="icon" className="md:hidden" onClick={()=>setMenu(true)}><Menu/></Button><Link to="/" className="mr-2 font-script text-4xl text-secondary link-focus">Make It Real</Link><nav className="hidden flex-1 items-center justify-center gap-2 md:flex">{nav.map(n=><NavLink key={n.to} to={n.to} className={({isActive})=>cn("rounded-full px-4 py-2 text-sm font-semibold transition-colors hover:bg-primary/30", isActive && "bg-primary/40 text-secondary")}>{n.label}</NavLink>)}</nav><form onSubmit={submit} className="hidden max-w-xs flex-1 items-center gap-2 rounded-full border border-border bg-background/60 px-3 py-1.5 lg:flex"><Search className="h-4 w-4 text-muted-foreground"/><Input value={q} onChange={(e)=>setQ(e.target.value)} placeholder="Search glow essentials" className="h-8 border-0 bg-transparent p-0 focus-visible:ring-0 focus-visible:ring-offset-0"/></form><Button variant="glass" size="sm" onClick={()=>setLanguage(language === "EN" ? "AR" : "EN")}>{language}</Button><Button asChild variant="glass" size="icon"><Link to="/wishlist" aria-label="Wishlist"><Heart/><span className="absolute -right-1 -top-1 rounded-full bg-accent px-1.5 text-[10px] font-bold text-accent-foreground">{wishlistIds.size}</span></Link></Button><Button asChild variant="glass" size="icon" className={cn(cartPulse && "animate-cart")}><Link to="/cart" aria-label="Cart"><ShoppingBag/><span className="absolute -right-1 -top-1 rounded-full bg-secondary px-1.5 text-[10px] font-bold text-secondary-foreground">{cartCount}</span></Link></Button><Button asChild variant="hero" size="sm"><Link to={user ? "/profile" : "/auth/login"}>{user ? user.name?.split(" ")[0] || "Profile" : "Login"}</Link></Button></div></header>{menu && <div className="fixed inset-0 z-50 bg-secondary/30 backdrop-blur-sm md:hidden"><aside className="h-full w-80 max-w-[86vw] bg-background p-5 shadow-warm"><div className="mb-8 flex items-center justify-between"><span className="font-script text-4xl text-secondary">Make It Real</span><Button variant="ghost" size="icon" onClick={()=>setMenu(false)}><X/></Button></div><nav className="grid gap-2">{nav.map(n=><Link key={n.to} to={n.to} onClick={()=>setMenu(false)} className="rounded-xl px-4 py-3 font-semibold hover:bg-primary/30">{n.label}</Link>)}{user && <button onClick={logout} className="rounded-xl px-4 py-3 text-left font-semibold hover:bg-primary/30">Logout</button>}</nav></aside></div>}<main><Outlet /></main><footer className="mt-20 border-t border-border bg-card/70"><div className="section-shell grid gap-8 py-12 md:grid-cols-4"><div><h3 className="font-script text-4xl text-secondary">Make It Real</h3><p className="mt-3 text-sm text-muted-foreground">Luxurious makeup, skincare, and tools for the glow you make your own.</p></div>{["Quick Links","Customer Service","Newsletter"].map((h,i)=><div key={h}><h4 className="mb-3 font-bold text-secondary">{h}</h4><div className="grid gap-2 text-sm text-muted-foreground">{i===0 && ["Shop","Categories","New Arrivals"].map(x=><Link key={x} to="/shop">{x}</Link>)}{i===1 && ["Free Returns","Secure Payment","Support"].map(x=><span key={x}>{x}</span>)}{i===2 && <><p>Beauty notes, offers, and launch alerts.</p><div className="flex gap-2"><Instagram/><Facebook/><Twitter/></div></>}</div></div>)}</div><div className="border-t border-border py-5 text-center text-sm text-muted-foreground">© 2026 Make It Real. All rights reserved.</div></footer><nav className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-5 border-t border-border bg-glass/90 py-2 backdrop-blur-xl md:hidden">{[["/",Home,"Home"],["/shop",Store,"Shop"],["/cart",ShoppingBag,"Cart"],["/wishlist",Heart,"Wish"],["/profile",User,"Profile"]].map(([to,Icon,label])=><Link key={String(to)} to={String(to)} className="flex flex-col items-center gap-1 text-xs text-muted-foreground"><Icon className="h-5 w-5" />{String(label)}</Link>)}</nav></div> }
